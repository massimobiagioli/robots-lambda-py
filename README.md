# Robots Lambda Python

## Setup

- Install NodeJs 14.x 
- Install Serverless Framework: <code>npm install -g serverless</code>
- Install Python 3.9
- Create python virtual environment: <code>python -v venv .venv</code>
- Install dependencies: <code>python -m pip install -r requirements.txt</code>